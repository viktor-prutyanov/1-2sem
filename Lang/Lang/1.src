{
	scan (a);
	scan (b);
	scan (c);
	d = b^2 - 4*a*c;
	if (d >= 0)
	{
		d = d ^ (1/2);
		c = 0;
		while (c <= 2)
		{
			print ((b*(-1) + d) / (2*a));
			print ((b*(-1) - d) / (2*a));
			inc (c);
		}
	}
}