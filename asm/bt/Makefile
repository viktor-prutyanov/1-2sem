all:
	@g++ -std=gnu++11 -g bt.cpp -Wparentheses -Wunused-value -Wformat && ./a.out && echo ''

build:
	@g++ -std=gnu++11 -g bt.cpp -Wparentheses -Wunused-value -Wformat && echo ''

clean:
	@rm -rf *.o a.out